node_modules
.DS_Store
dist
dist-ssr
*.local
node_modules/*
.env

# Standardized on pnpm — do not commit the npm lockfile
package-lock.json

.cursorrules

# TypeScript build cache
tsconfig.tsbuildinfo

# Playwright login credentials for tests/playwright -- real account
# credentials, never commit (*.local alone doesn't match *.local.json)
tests/playwright-credentials.local.json
# Saved Playwright session (live auth token), regenerated by auth.setup.ts
tests/playwright/.auth/
# Playwright's own run artifacts
/test-results/
/playwright-report/